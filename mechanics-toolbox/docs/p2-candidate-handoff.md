# P2 开发候选完成（待工程签收）

日期：2026-07-19  
分支：`phase/p2-structural-mechanics`  
工作树：`F:\mechanics\.worktrees\p2`

## 本候选范围

- 1D 梁从首阶段使用节点自由度 `u/v/θ`；支持等 `E/A/I` 单跨梁与已确认的固定—简支/两端固支边界。
- 简支梁反力、截面剪力/弯矩/位移影响线，以及单轴组移动荷载控制位置。
- 平面桁架节点荷载、均匀温差、初应变和杆件自重。
- 平面刚架节点荷载、局部全跨/区间常值分布荷载、均匀温差、初应变、端力、`N/V/M` 与指定纤维应力。
- 离线单 HTML UI：通用模型编辑、工程/SI 单位原子切换、结构图及六类图层开关、控制值/明细/曲线/公式区。
- 明确排除：分段变 `E/A/I`、连续多跨梁、内部铰/端释放、三角形/梯形分布荷载、温度梯度/热弯曲、3D 与机械接口。

## 本轮关键提交

- `be8fd12`：补齐 P2 UI 算例、结果适配与结构图。
- `c1af1a1`：补齐桌面/移动/来源/性能验收测试。
- `018358e`：补齐公式与性能追溯文档。
- `440f3b1`：收紧单位按钮、显示精度、载荷图层、坐标标签和公式元数据语义。
- `ce7369b`：严格执行标签零碰撞、外部请求为零、显示真值与结果公式追溯。
- `2703bf9`：同步候选状态和最终 QA 口径。
- `16b9436`：锁定刚架候选在正式裁决前的 `beta` 状态测试。
- 共享接入保持独立提交：`cc7aa32 integration: expose P2 structural workspace in app`。

## 可信度与验证

- 42 个冻结 fixture ID 一一对应；UI 13 个算例均记录 case ID、fixture 路径与版本。
- 结果 metadata 的公式 ID/version 与模块理论目录、`p2-index.md` 一致。
- 独立对照：SymPy 1.14、LibreOffice Calc 24.2 桁架与刚架对照通过；项目实现输出未用于回填真值。
- `npm run typecheck`：通过。
- `npm run test`：75 文件、457 项通过。
- `npm run build`：通过；产物为离线单 HTML。
- `npm run test:e2e`：32 项声明，30 通过，2 项按桌面/移动专属视口跳过。
- 最大确认模型：100 节点、180 单元、297 自由自由度；专用性能记录见 `docs/p2-performance.md`，候选决定为不引入 Worker。

## 已确认裁决

1. 用户确认 `FRAME-A01` 冻结端力数值属于 member resisting actions `kd-f`；element-on-node 固定为 `f-kd`。数值不变，只重标字段语义；`P2-G09/Gate P2-4B` 可关闭。
2. 用户确认沿用性能记录中的当前门槛，P2 首版不引入 Web Worker；`P2-K05` 可关闭。
3. 用户确认 `P2-K07` 公式、算例与来源审计：公式 ID/version 与结果 metadata/理论目录/索引一致，42 个 fixture 来源独立，无本地参考书/PDF读取，无项目实现输出回填真值。

## 尚未签收的项目

1. 当前只余 `P2-K09` 最终工程签收；必须由用户/力学负责人记录签收人、日期、遗留项和变更项。本文件仍只表示“P2 开发候选完成”，不得据此提前宣称全 P2 完成。
2. 历史质量项“小步提交；源码、测试、批量格式化不混为一个提交”未能对全部既有提交逐项证明，须作为过程遗留如实披露，不通过改写文档或补造提交历史冒充闭环。

## 集成影响

- 不修改 P3/P4 范围，不创建 remote，不推送。
- P1 单位目录仅通过独立 integration 提交增加结构所需柔度量纲；P1 全量回归通过。
- 刚架端力语义与 `Gate P2-4B` 已获确认；五个已通过的 P2 结构模块均显示为 `available`。`P2-K09` 是否签收仍由任务表与签收记录单独表达。
